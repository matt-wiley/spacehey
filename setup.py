from setuptools import setup, find_packages

setup(
    name='spacehey_maker',
    version=f"0.0.1",
    url='',
    author='',
    author_email='',
    description='',
    packages=find_packages(),    
    install_requires=[],
)
